# Gemini UI 回归修复（#161）

## 基线与范围

基于 Gemini UI 分支 `c736db1`，在 `codex/df-161-ui-regressions` 独立工作树修复；
不切换或写入 Gemini 的工作目录，不将其尚未完成的 UI 批次直接合入 main。
本分支不包含后续 main 的 Room 升级，合并基线变化后仍须重新验证，不能沿用该升级的测试结果。

保留细线进度、卡片布局与指标详情视觉方向，仅修复审查确认的信息保留和验证问题：

- 长期进度恢复准确完成数/目标数，附带可访问进度语义；细线仍保留，比例截到 0–1，实际计数不截断。
- 指标列表恢复增加/减少/区间方向及可访问名称，空记录时也可查看已配置方向。
- 指标详情的目标/上下界不再依赖已有记录，并恢复单位/精度摘要；不恢复笨重的信息卡。
- 更新旧布局断言，保留并重新执行录入、备注、outbox、重开和删除确认断言，新增空记录目标回归。
- 仅忽略根 `screenshots/` 和既有 captures/build 输出，不全局忽略 PNG；仓库测试验证图片资源可跟踪。
- 用户于本轮明确确认删除“已暂停”是有意的视觉变更，保留 Gemini 的无后缀显示；更新中英文格式测试，
  仍检查时间、目标分钟和参数顺序，不改变暂停/恢复状态机或按钮动作。

不修改领域逻辑、账户、数据库、同步协议、计时规则、依赖或测试预算。留白、字号、暂停配色及完整
手机/平板视觉验收仍由 UI 工作继续处理；本次没有承诺它们已经通过。

## 验证

修复前真机专项 8 项中 3 项失败：空指标卡仍按旧布局断言；两个详情交互用例在重复标题断言停止，
未进入录入和删除流程。不能将这些失败解释为已证实业务数据损坏。

新增大字体测试最初报告段落宽度与文字组件测量宽度不一致，给进度文字明确的内容区全宽后修正；
保留溢出断言，并在 240dp 宽度、2 倍字体下复测。首次专项 17 项中的其余 16 项通过，失败日志保留。

修正后专项 17 项全部通过（0 失败/错误/跳过），编译告警 0 / 允许 0；仓库检查 22 项通过。
首次完整运行在既有暂停文案断言失败后主动停止；该局部运行不计为完整验证。
起初将暂停后缀恢复属于误判，用户澄清后已撤回：此处应更新旧测试，不把已批准的文案删除列为缺陷。
紧接取消后的专项发生 testbed 被卸载、进程被杀的中断（日志含包移除事件，与旧运行延迟清理相符）；
没有将其记为通过或修改业务代码掩盖中断，保留局部证据并在旧进程退出后重跑。
按用户确认的无暂停后缀设计，最终专项 20 项通过。完整物理设备套件 739 项，0 失败、0 错误、
0 跳过，instrumentation 耗时 767.457 秒。逐项对比基线 731 个用例：仅有 1 项空卡布局用例更名，
新增 8 项，无其他缺失或重复；输入校验、真实 Room 仓库和 TimerService 覆盖率校准通过。
完整检查随后由 lint 检出 Gemini 遗留的 4 个无引用字符串；确认无调用后从中英文资源移除，
未修改 lint 基线。上述 739 项结果发生在这次资源清理前，不表述为清理后再次完整运行。
资源清理后 `./tools/verify android-build` 通过：构建成功、lint 无新增问题，编译告警 0 / 允许 0，
仓库检查 22 项通过；20 项相关真机专项再次全部通过，结果校验无失败、错误或跳过，
本轮专项日志未出现关闭连接池或致命异常记录。既有 lint 基线仍保留，不表示全部历史问题已清零。
所有 Android 测试使用隔离 `com.dayforge.testbed` 物理设备，
不覆盖正式 App，不使用模拟器或 JVM/Robolectric，不运行 Docker。
原始日志保存在仓库外 `DayForge-test-audit-2026-09-23/ui-161/`，不提交设备标识、APK 或运行数据。
合入 Gemini 工作目录时须审查原先被全局规则隐藏的 PNG；临时截图应放进 `screenshots/`，
不自动提交旧截图或将它们当作正式素材。本批不删除原工作目录文件。

## 未解决的测试隔离观察

完整日志在 `EditHabitViewModelScheduleDaysTest.loadHabit_weeklyHabit_setsOriginalScheduleDaysTo7`
期间出现一次 `WidgetRefresher` 访问已关闭连接池并重试的记录，附近还有测试数据库被删除时仍有连接的提示。
该后台路径不在本批 UI 修改中；目前不能证明其请求来源，也不能直接认定它等同 main 已修复的设置夹具问题。
本批未吞掉异常、禁用刷新或改动生产数据库生命周期，应后续单独定位请求所属用例并收束后台任务生命周期。
因此“739 项断言通过”不等于整套日志完全正常，也不是该后台风险已解决的声明。

## 回退

只撤回本批 UI/测试/文档修复，不回退 Gemini 原有视觉提交，不清除或迁移数据。
