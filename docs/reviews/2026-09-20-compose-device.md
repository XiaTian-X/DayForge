# Compose 与系统栏测试审查（#143）

## 范围与独立预期

迁移 DayForgeWindow（9）、DayForgeSystemBars（3）、TrendChart（3）、MetricDetailScreen（4），
共 19 个历史方法；新增趋势聚合交互与两个真实指标页面场景，共 22 项。

- 窗口测试保留所有几何关系和固定 dp 预期。旧 Robolectric 强制 400dp 宽度改为真机根布局与内容
  边界相等且宽度大于零，检查包装 Context 下主题仍填满可用空间。
- 系统栏使用真实 ActivityScenario，在主线程读写窗口；临时资源夜间标记在 finally 恢复。
  真实 API 35 验证图标对比度、透明栏和导航栏保护；旧配置列表中的 API 26/29 未执行。
- 趋势图旧测试仅验证枚举，无法证明默认选中和切换正确。保留枚举契约，增加实际 FilterChip 选择状态、
  7/30 天双向切换、聚合菜单点击恰好发出 SUM、外部配置变更更新显示且不额外发送回调。
- 指标详情旧测试仅验证数据类，不计作页面行为覆盖。新增真实 Compose 页面、ViewModel、仓库与生产
  磁盘 Room：输入固定 12.5 kg 和备注，检查历史呈现、原始持久化行与两个 outbox 操作；关闭 ViewModel
  并重开数据库再次精确比较。取消删除保留事实/队列且无导航回调，确认删除移除指标与记录、产生删除
  outbox，导航回调恰好一次。未替换 Android 主调度器。

## 夹具修正与验证

首次运行指标详情类因 Kotlin 清理方法返回 nullable Unit 而不符合 JUnit void 约定，类未执行；
其余 16 项通过。修正后实际页面暴露重复文字定位歧义：标题与摘要同名，弹窗标题与确认按钮同名。
选择器分别验证两个名称节点，并按弹窗内可点击按钮定位，不更改产品文字或添加仅供测试的产品路径。
修正后指标详情 6 项通过。失败日志、XML 与成功重跑均归档。

三个可编译错误实现均被真机断言检出：默认选中 30 天、丢弃聚合回调、记录时丢弃备注，源码均按字节恢复。
完整真机 659 项通过，0 失败/错误/跳过；lint、构建、警告 0、覆盖率校准、仓库 12 项通过。
首次完整门禁再次在未修改的旧 AccountLocalStateCleanerTest 遇到 lint 内部 FIR 异常；同源码完整重跑通过，
未关闭规则或将失败算为通过。同阶段后端完整重跑 703 项通过、92% 行覆盖率，1 项已登记开发配置警告；Ruff、格式、现行类型检查与 OpenAPI 通过。
证据路径：`/Users/xbase/workspace/DayForge-test-audit-2026-09-20/compose-143/`。

## 未验证边界

仅 MI 6 / API 35 真机。窗口测试主动提供可重复的 Insets，不能宣称真实键盘、物理旋转、切口设备或
三键/手势导航模式切换均已验收。其他 API/OEM、图表像素质量、桌面、外网/NAS 与服务器联合验收未执行。
本批无产品代码、依赖、时间语义、账户或协议修改；没有 JVM/Robolectric 或模拟器执行。
